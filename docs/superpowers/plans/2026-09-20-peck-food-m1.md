# peck-food M1 实施计划（habit-checkin + habit-enforcer + meme-buddy 本地闭环）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 PRD M1：三个 skill（checkin/enforcer/meme-buddy）+ 零依赖状态与判定脚本 + 本地表情包底座 + Hermes 接线指南，覆盖 PRD §8 验收场景 1–5。

**Architecture:** 寄生架构（PRD §3.1）。确定性逻辑（schema、streak、tick 判定、幂等、免打扰、补卡券、熔断计数）全部在 `scripts/state.py`（纯标准库），LLM 通过 SKILL.md 编排脚本输出并做人设渲染与配图；`tick_check.py --now` 支持时间注入使行为核心可全自动测试。

**Tech Stack:** Python 3.9+（仅标准库 json/datetime/argparse/unittest）、Markdown（SKILL.md 体系，Agent Skills 开放标准）、SVG（占位表情，纯文本生成）。

**Spec:** `prd.md`（v1.0，commit 04bc169）——执行者必须同时读过 PRD 的 §3、§4、§5、§8 再动手。

## Global Constraints

- 全部脚本仅 Python 标准库，零第三方依赖（宿主环境不可假设 pip 可用）。
- 状态文件为唯一共享状态：`state/habits.json`，schema 见 PRD §3.3，原子写（temp+rename）。
- 一切时间计算基于 `user.timezone`（Asia/Shanghai 等IANA 名，用 `datetime.now(ZoneInfo)`）。
- SKILL.md 遵循 Agent Skills 开放标准：YAML frontmatter 仅 `name` + `description` 两个字段，description 写成"何时用我"的触发条件（PRD §3.2）。
- 防骚扰硬约束（PRD §4.2）：单习惯单日提醒/催促 ≤ 4 条；免打扰 22:30–07:00（晚间段 + 清晨段）优先级最高，只拦会发消息的动作（`daily_close` 静默记账豁免）；tick 幂等以 `nudge_state` 为准。
- M1 档位范围：strict + chill；free 档、日报、成就颁发、在线 provider 均为 M2，不做不写。
- 中文文案为主（人设、模板、文档）；代码标识符与注释英文。
- 提交信息用 conventional commits（feat:/test:/docs:/chore:）。

## File Structure（M1 最终形态）

```
peck-food/
├── scripts/
│   ├── state.py             # 状态库：load/save/validate/streak/apply_checkin/plan_tick/commit_tick
│   ├── tick_check.py        # CLI 入口：打印本轮动作 JSON（--now 注入时间）
│   └── validate_state.py    # CLI 入口：schema 校验，非法退出码 1
├── skills/
│   ├── habit-enforcer/
│   │   ├── SKILL.md
│   │   └── references/
│   │       ├── personas.md      # strict/chill 两档人设 + 模板池（各 10 条）
│   │       └── escalation.md    # 轮次/上限/免打扰/幂等/熔断规则
│   ├── habit-checkin/
│   │   ├── SKILL.md
│   │   └── references/state-schema.md
│   └── meme-buddy/
│       ├── SKILL.md
│       └── assets/memes/
│           ├── _index.json
│           ├── urge/ praise/ disappointed/ angry/ cute/ celebrate/   # 各 1 张 SVG 占位
│           └── user/
├── tests/
│   ├── test_state.py
│   ├── test_checkin.py
│   └── test_tick.py
├── docs/
│   ├── state-schema.md
│   ├── host-hermes.md
│   └── superpowers/plans/2026-09-20-peck-food-m1.md   # 本计划
├── state/habits.json        # 初始空状态（user 信息占位）
└── README.md
```

---

### Task 1: 状态库核心（schema + 原子读写 + 校验 CLI）

**Files:**
- Create: `scripts/state.py`
- Create: `scripts/validate_state.py`
- Create: `state/habits.json`（合法空状态种子）
- Test: `tests/test_state.py`

**Interfaces:**
- Produces: `load(path:str) -> dict`、`save(path:str, state:dict) -> None`（原子写）、`validate(state:dict) -> list[str]`（空列表=合法）、`empty_state(name:str, tz:str) -> dict`、CLI：`python3 scripts/validate_state.py [path]`（合法退出 0 并打印 OK，非法退出 1 打印错误清单）。
- 后续任务依赖这些签名；`habits.json` 顶层键与 PRD §3.3 完全一致：`version, user, global_level, habits, nudge_state, persona_state, achievements, tick_log`。

- [x] **Step 1: 写失败测试**

```python
# tests/test_state.py
import json, os, tempfile, unittest
import sys; sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'scripts'))
from state import load, save, validate, empty_state

class TestStateCore(unittest.TestCase):
    def test_empty_state_valid(self):
        self.assertEqual(validate(empty_state("老板", "Asia/Shanghai")), [])

    def test_validate_catches_missing_top_keys(self):
        st = empty_state("老板", "Asia/Shanghai"); del st["nudge_state"]
        errs = validate(st)
        self.assertTrue(any("nudge_state" in e for e in errs))

    def test_validate_catches_bad_level(self):
        st = empty_state("老板", "Asia/Shanghai")
        st["global_level"] = "tyrant"
        self.assertTrue(any("global_level" in e for e in errs := validate(st)))

    def test_validate_catches_habit_fields(self):
        st = empty_state("老板", "Asia/Shanghai")
        st["habits"].append({"id": "h_x"})  # 缺字段
        self.assertTrue(validate(st))

    def test_save_is_atomic_and_readable(self):
        with tempfile.TemporaryDirectory() as d:
            p = os.path.join(d, "h.json")
            save(p, empty_state("老板", "Asia/Shanghai"))
            self.assertEqual(load(p)["user"]["name"], "老板")
            self.assertFalse(os.path.exists(p + ".tmp"))  # 临时文件已清理

if __name__ == "__main__":
    unittest.main()
```

- [x] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_state -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'state'`）

- [x] **Step 3: 最小实现**

```python
# scripts/state.py  （Task 1 部分；业务逻辑后续任务追加）
import json, os, tempfile

REQUIRED_TOP = ["version", "user", "global_level", "habits", "nudge_state",
                "persona_state", "achievements", "tick_log"]
REQUIRED_USER = ["id", "name", "timezone", "quiet_hours"]
REQUIRED_HABIT = ["id", "name", "emoji", "schedule", "level", "require_proof",
                  "weekly_goal", "stats", "records", "pauses", "freeze_left"]
LEVELS = {"strict", "chill", "free"}

def empty_state(name, tz):
    return {"version": 1,
            "user": {"id": "default", "name": name, "timezone": tz,
                     "quiet_hours": ["22:30", "07:00"]},
            "global_level": "chill", "habits": [],
            "nudge_state": {"per_habit_day": {}, "no_response_days": {}},
            "persona_state": {"recent_templates": [], "recent_memes": []},
            "achievements": [], "tick_log": []}

def validate(st):
    errs = []
    for k in REQUIRED_TOP:
        if k not in st: errs.append(f"missing top key: {k}")
    if errs: return errs
    for k in REQUIRED_USER:
        if k not in st["user"]: errs.append(f"user missing {k}")
    if st["global_level"] not in LEVELS: errs.append("global_level invalid")
    for h in st["habits"]:
        for k in REQUIRED_HABIT:
            if k not in h: errs.append(f"habit {h.get('id','?')} missing {k}")
        if h.get("level") is not None and h["level"] not in LEVELS:
            errs.append(f"habit {h.get('id','?')} level invalid")
        win = h.get("schedule", {}).get("window")
        if not (isinstance(win, list) and len(win) == 2):
            errs.append(f"habit {h.get('id','?')} window invalid")
    return errs

def load(path):
    with open(path, encoding="utf-8") as f: return json.load(f)

def save(path, state):
    d = os.path.dirname(os.path.abspath(path)) or "."
    fd, tmp = tempfile.mkstemp(dir=d, suffix=".tmp")
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            json.dump(state, f, ensure_ascii=False, indent=2)
        os.replace(tmp, path)
    finally:
        if os.path.exists(tmp): os.remove(tmp)
```

```python
# scripts/validate_state.py
import sys, os
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from state import load, validate

def main():
    path = sys.argv[1] if len(sys.argv) > 1 else os.path.join(
        os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "state", "habits.json")
    errs = validate(load(path))
    if errs:
        print("\n".join(errs)); sys.exit(1)
    print("OK")

if __name__ == "__main__":
    main()
```

同时创建 `state/habits.json`：内容为 `empty_state("老板", "Asia/Shanghai")` 的输出（可直接 `python3 -c` 生成后提交）。

- [x] **Step 4: 跑测试确认通过**

Run: `python3 -m unittest tests.test_state -v && python3 scripts/validate_state.py`
Expected: 全部 PASS，CLI 输出 `OK`

- [x] **Step 5: Commit**

```bash
git add scripts/state.py scripts/validate_state.py state/habits.json tests/test_state.py
git commit -m "feat: 状态库核心——schema/校验/原子读写与 CLI"
```

---

### Task 2: 打卡业务逻辑（streak / 迟到 / 补卡券 / 请假 / 周计数）

**Files:**
- Modify: `scripts/state.py`（追加函数）
- Test: `tests/test_checkin.py`

**Interfaces:**
- Consumes: Task 1 的 `load/save/validate`。
- Produces:
  - `effective_level(state, habit) -> str`（`habit["level"] or state["global_level"]`）
  - `find_habit(state, name) -> dict | None`（按 id 或名称模糊匹配）
  - `is_due(habit, day: date) -> bool`（schedule.days 含该周几且不在 pauses 区间）
  - `apply_checkin(state, habit_id, day: date, note:str, proof:bool, now: datetime) -> dict`
    返回 `{"ok": bool, "reason": str|None, "streak": int, "late": bool, "makeup": bool}`；直接改 state（调用方负责 save）。
  - `reset_monthly_freeze(state, today: date) -> None`（仅每月 1 号把 freeze_left 重置为 2，当日幂等）

- [x] **Step 1: 写失败测试**

```python
# tests/test_checkin.py
import os, sys, unittest
from datetime import date, datetime
from zoneinfo import ZoneInfo
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'scripts'))
from state import empty_state, apply_checkin, effective_level, is_due

TZ = ZoneInfo("Asia/Shanghai")
def mkstate():
    st = empty_state("老板", "Asia/Shanghai")
    st["habits"].append({
        "id": "h_run", "name": "跑步", "emoji": "🏃",
        "schedule": {"days": [1,2,3,4,5,6,0], "window": ["06:00","22:00"]},
        "level": None, "require_proof": False, "weekly_goal": 3,
        "stats": {"current_streak": 0, "best_streak": 0, "total": 0, "week_count": 0},
        "records": [], "pauses": [], "freeze_left": 2})
    return st

class TestCheckin(unittest.TestCase):
    def test_effective_level_inherits_global(self):
        st = mkstate(); self.assertEqual(effective_level(st, st["habits"][0]), "chill")

    def test_ontime_checkin_grows_streak(self):
        st = mkstate()
        r = apply_checkin(st, "h_run", date(2026,9,18), "5km", True,
                          datetime(2026,9,18,7,0,tzinfo=TZ))
        self.assertTrue(r["ok"]); self.assertEqual(r["streak"], 1)
        self.assertFalse(r["late"])

    def test_late_checkin_keeps_streak_marks_late(self):
        st = mkstate()
        r = apply_checkin(st, "h_run", date(2026,9,18), "", False,
                          datetime(2026,9,18,23,0,tzinfo=TZ))   # 窗口 22:00 结束后
        self.assertTrue(r["ok"]); self.assertTrue(r["late"]); self.assertEqual(r["streak"], 1)

    def test_double_checkin_same_day_rejected(self):
        st = mkstate()
        apply_checkin(st, "h_run", date(2026,9,18), "", False, datetime(2026,9,18,8,0,tzinfo=TZ))
        r = apply_checkin(st, "h_run", date(2026,9,18), "", False, datetime(2026,9,18,9,0,tzinfo=TZ))
        self.assertFalse(r["ok"]); self.assertEqual(r["reason"], "already checked in")

    def test_makeup_yesterday_costs_freeze_only_in_chill(self):
        st = mkstate()
        r = apply_checkin(st, "h_run", date(2026,9,17), "补卡", False,
                          datetime(2026,9,18,9,0,tzinfo=TZ))
        self.assertTrue(r["ok"]); self.assertTrue(r["makeup"])
        self.assertEqual(st["habits"][0]["freeze_left"], 1)
        st["global_level"] = "strict"
        r2 = apply_checkin(st, "h_run", date(2026,9,16), "补卡", False,
                           datetime(2026,9,18,9,0,tzinfo=TZ))
        self.assertFalse(r2["ok"]); self.assertIn("strict", r2["reason"])

    def test_makeup_needs_freeze_ticket(self):
        st = mkstate(); st["habits"][0]["freeze_left"] = 0
        r = apply_checkin(st, "h_run", date(2026,9,17), "", False,
                          datetime(2026,9,18,9,0,tzinfo=TZ))
        self.assertFalse(r["ok"])

    def test_pause_day_not_due(self):
        st = mkstate()
        st["habits"][0]["pauses"].append({"from":"2026-09-18","to":"2026-09-18","reason":"病"})
        self.assertFalse(is_due(st["habits"][0], date(2026,9,18)))

if __name__ == "__main__":
    unittest.main()
```

- [x] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_checkin -v`
Expected: FAIL（`ImportError: cannot import name 'apply_checkin'`）

- [x] **Step 3: 实现（追加到 state.py）**

判定规则（与 PRD §4.1 一一对应）：
- `is_due`：`(day.weekday()+1)%7 in schedule["days"]`（周一=1 … 周六=6，**周日=0**）且日期不在任何 pause 的 [from, to] 闭区间。
- `apply_checkin`：
  - 同日重复 → `{"ok":False,"reason":"already checked in"}`。
  - 目标日 == 今天：late = now 时间 > 窗口结束；记账 `{date, proof, note, late}`；streak 依据"上一 due 日是否已有 record"连续累加（从 records 重建：`current_streak = 1 + 之前连续 due 日链长`，跳过非 due 日）；`week_count`（周一为界）与 `total` 同步 +1。
  - 目标日 == 昨天：仅 `effective_level == "chill"` 且 `freeze_left > 0` 时受理，`makeup=True`，`freeze_left -= 1`；streak 重建逻辑同上。
  - 其他日期 → `{"ok":False,"reason":"out of range"}`。
- streak 重建函数 `recompute_streak(habit, upto: date) -> int`：从 upto 往回逐日走，due 且有 record 则 +1 继续；due 无 record 则停；非 due 日跳过。`best_streak = max(best, current)`。
- `reset_monthly_freeze`：**仅每月 1 号**（`today.day == 1`）把每个 habit 的 `freeze_left` 重置为 2，其余日期直接返回（当日幂等，tick 每次调用）。

- [x] **Step 4: 跑测试确认通过**

Run: `python3 -m unittest tests.test_checkin tests.test_state -v`
Expected: 全部 PASS

- [x] **Step 5: Commit**

```bash
git add scripts/state.py tests/test_checkin.py
git commit -m "feat: 打卡业务逻辑——streak 重建/迟到/补卡券/请假/周计数"
```

---

### Task 3: tick 判定引擎（提醒/催促轮次/断链日结/周报/幂等/免打扰/熔断/上限）

**Files:**
- Modify: `scripts/state.py`（追加）
- Create: `scripts/tick_check.py`
- Test: `tests/test_tick.py`

**Interfaces:**
- Consumes: Task 1/2 全部；`is_due/effective_level/recompute_streak/reset_monthly_freeze`。
- Produces:
  - `plan_tick(state, now: datetime) -> list[dict]`：**纯函数，不改 state**。每个 action 形如
    `{"type": "nudge|daily_close|weekly_report|level_down", "habit_id": str|None, "stage": "remind|first|warn|final|lastcall|none", "mood": "urge|disappointed|angry|cute|celebrate", "slots": {...}}`（`daily_close` **无 mood 字段**：静默记账、不渲染消息）
  - `commit_tick(state, actions, now) -> None`：把已发送动作写入 `nudge_state.per_habit_day`（count 与 stages 集合）、追加 `tick_log`（裁剪至 50 条）、更新 `persona_state` 由 LLM 侧另行写入。
  - CLI：`python3 scripts/tick_check.py [--now "2026-09-20T21:00+08:00"] [--commit]`——默认只打印
    `{"now":..., "actions":[...]}`；`--commit` 时先 plan，再写回 state。
- 后续 Task 6 的 enforcer SKILL.md 依赖该 CLI 的输出契约。

- [x] **Step 1: 写失败测试**

```python
# tests/test_tick.py
import os, sys, unittest
from datetime import date, datetime
from zoneinfo import ZoneInfo
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'scripts'))
from state import empty_state, plan_tick, commit_tick

TZ = ZoneInfo("Asia/Shanghai")
def mkstate(level="strict"):
    st = empty_state("老板", "Asia/Shanghai"); st["global_level"] = level
    st["habits"].append({
        "id": "h_run", "name": "跑步", "emoji": "🏃",
        "schedule": {"days":[1,2,3,4,5,6,0],"window":["07:00","22:00"]},
        "level": None, "require_proof": False, "weekly_goal": 3,
        "stats": {"current_streak":4,"best_streak":12,"total":30,"week_count":2},
        "records": [], "pauses": [], "freeze_left": 2})
    return st

def t(h, m): return datetime(2026,9,21,h,m,tzinfo=TZ)  # 2026-09-21 是周一

def nudges(acts): return [a for a in acts if a["type"]=="nudge"]

class TestTick(unittest.TestCase):
    def test_strict_window_open_remind(self):
        acts = plan_tick(mkstate("strict"), t(7,0))
        self.assertIn("remind", [a["stage"] for a in nudges(acts)])

    def test_strict_first_warn_final_stages(self):
        self.assertIn("first", [a["stage"] for a in plan_tick(mkstate("strict"), t(8,0))])
        self.assertIn("warn", [a["stage"] for a in plan_tick(mkstate("strict"), t(10,0))])
        self.assertIn("final", [a["stage"] for a in plan_tick(mkstate("strict"), t(21,30))])

    def test_idempotent_same_stage_once(self):
        st = mkstate("strict")
        acts1 = plan_tick(st, t(8,0)); commit_tick(st, acts1, t(8,0))
        acts2 = plan_tick(st, t(8,30))
        self.assertNotIn("first", [a["stage"] for a in nudges(acts2)])

    def test_daily_cap_four(self):
        st = mkstate("strict")
        for hm in [(7,0),(8,0),(10,0),(21,30)]:   # remind+first+warn+final 恰好用满 4 条
            commit_tick(st, plan_tick(st, t(*hm)), t(*hm))
        extra = plan_tick(st, t(21,59))
        self.assertEqual(nudges(extra), [])

    def test_quiet_hours_block_and_strict_pulls_final_earlier(self):
        st = mkstate("strict")
        st["habits"][0]["schedule"]["window"] = ["21:00", "23:00"]  # final 名义时刻 22:30 撞免打扰
        self.assertIn("final", [a["stage"] for a in plan_tick(st, t(22,0))])  # 积压 stage 合并后提前补发
        self.assertEqual(nudges(plan_tick(mkstate("strict"), t(23,0))), [])

    def test_morning_quiet_blocks_then_postpones_to_seven(self):
        st = mkstate("strict")
        st["habits"][0]["schedule"]["window"] = ["06:00", "22:00"]
        # 00:00–07:00 清晨段同样免打扰：窗口从 06:00 开也不发
        self.assertEqual(plan_tick(st, t(6,30)), [])
        # 窗口开启提醒顺延到 ≥max(窗口起, 07:00) 的第一个 tick，而非作废
        self.assertTrue(nudges(plan_tick(st, t(7,0))))

    def test_chill_midday_and_lastcall(self):
        st = mkstate("chill")  # 窗口 07-22：中点 14:30，结束前 1h = 21:00
        self.assertEqual([a["stage"] for a in nudges(plan_tick(st, t(14,30)))], ["remind"])
        self.assertEqual(nudges(plan_tick(st, t(10,0))), [])
        self.assertIn("lastcall", [a["stage"] for a in nudges(plan_tick(st, t(21,0)))])

    def test_daily_close_breaks_streak_strict(self):
        st = mkstate("strict")
        acts = plan_tick(st, t(23,50))
        closes = [a for a in acts if a["type"]=="daily_close"]
        self.assertTrue(closes)  # 当日 due 未打卡 → 断链日结动作

    def test_daily_close_is_silent_bookkeeping(self):
        st = mkstate("strict")
        acts = plan_tick(st, t(23,50))
        closes = [a for a in acts if a["type"]=="daily_close"]
        self.assertTrue(closes)
        for a in closes:
            self.assertIn("slots", a)      # 静默记账仍带槽位（归档用）
            self.assertNotIn("mood", a)    # 无 mood → 不渲染消息、不配图
        commit_tick(st, acts, t(23,50))
        self.assertEqual(st["habits"][0]["stats"]["current_streak"], 0)  # 断链归零（PRD §4.2）

    def test_weekly_report_saturday_ten(self):
        # 2026-09-26 是周六
        acts = plan_tick(mkstate("chill"), datetime(2026,9,26,10,0,tzinfo=TZ))
        self.assertIn("weekly_report", [a["type"] for a in acts])

    def test_level_down_after_three_silent_days(self):
        st = mkstate("strict")
        st["nudge_state"]["no_response_days"] = {"h_run": 3}
        acts = plan_tick(st, t(8,0))
        self.assertIn("level_down", [a["type"] for a in acts])

if __name__ == "__main__":
    unittest.main()
```

- [x] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest tests.test_tick -v`
Expected: FAIL（ImportError）

- [x] **Step 3: 实现（state.py 追加 + tick_check.py）**

判定表（`plan_tick` 内部，逐习惯）：
1. `reset_monthly_freeze`；`no_response_days[h_id] >= 3` 且 level==strict → 产出 `level_down`（写 `habit["level"]="chill"` 的责任在 commit；明示理由 slot）。
2. 免打扰（22:30–24:00 与 00:00–07:00 两段，跨零点区间判断）：拦 nudge/weekly_report/level_down 产出；`daily_close` 豁免但静默记账。
3. 当日 due 且未打卡：
   - strict 四轮（PRD §4.2"提醒 1 + 催促 3"）：窗口开启后第一个 tick（窗口起早于 07:00 时顺延至 ≥max(窗口起, 07:00)）→ `remind`（urge）；窗口起+1h → `first`（urge）；窗口起+3h → `warn`（disappointed）；窗口结束前 30min → `final`（angry）。名义时刻落入免打扰的 stage：清晨段顺延至 07:00、晚间段提前至 22:30 前最后一个可发 tick，**多个积压 stage 在同一 tick 合并为最高优先级一条**（final > warn > first > remind）。已发过的 stage（`per_habit_day[h@d]["stages"]` 集合）跳过；当日 count≥4 全跳过（四轮恰好用满上限）。
   - chill 两轮：窗口中点后的第一个 tick → `remind`（cute）；窗口结束前 1h → `lastcall`（cute）。落入免打扰一律作废不补发（不提前、不顺延）。同 stage 幂等。
   - mood 映射：remind→strict:urge/chill:cute，first→urge，warn→disappointed，final→angry，lastcall→cute。
4. 当日 due 且 23:30 后仍无 record → `daily_close`（仅 strict：**静默记账**——无 `mood` 字段、不发消息不配图，免打扰豁免；commit 时把 `stats.current_streak` 归零，处刑落在周日公审周报）；chill 不断链（周目标制，不产出 `daily_close`，周日 23:30 的统计仅作 `weekly_report` 前置数据）。
5. 周报：chill 周六 10:00–10:29 的第一个 tick → `weekly_report`（slots：完成率、streak 榜 JSON）；strict 周日 21:00（M1 含，PRD §4.2 公审周报归 M1 范围"周报"）。
6. 已打卡的习惯不产出任何 nudge；当日有 record 则 `no_response_days[h_id]=0`，否则 +1（commit 时计算）。

`tick_check.py`：argparse 解析 `--now`（ISO 格式，缺省 `datetime.now(tz)`）与 `--commit`；plan 后打印 `json.dumps({"now":..., "actions":...}, ensure_ascii=False)`；`--commit` 时调用 `commit_tick` 并 `save`。

- [x] **Step 4: 跑测试确认通过 + CLI 冒烟**

Run: `python3 -m unittest discover tests -v && python3 scripts/tick_check.py --now "2026-09-21T07:00:00+08:00"`
Expected: 全部 PASS；种子状态（空习惯）下 CLI 打印 `"actions": []`；临时给 `state/habits.json` 加一条窗口 `["07:00","22:00"]` 的 strict 习惯后同一命令打印含 `"stage": "remind"` 的 JSON（07:00 正是窗口开启的第一个 tick；验完还原）

- [x] **Step 5: Commit**

```bash
git add scripts/state.py scripts/tick_check.py tests/test_tick.py
git commit -m "feat: tick 判定引擎——轮次/幂等/免打扰/断链/周报/熔断与 CLI"
```

---

### Task 4: meme-buddy skill（本地包 + kaomoji 底座 + user 目录机制）

**Files:**
- Create: `skills/meme-buddy/SKILL.md`
- Create: `skills/meme-buddy/assets/memes/_index.json`
- Create: `skills/meme-buddy/assets/memes/{urge,praise,disappointed,angry,cute,celebrate,user}/`（各放 1 张 SVG 占位图，文本 SVG：大字情绪词 + kaomoji）
- Create: `scripts/gen_placeholder_memes.py`（生成上述 SVG，可重复执行）

**Interfaces:**
- Produces（对 enforcer/checkin 的 SKILL.md 而言）：按 PRD §4.5 选图协议——输入 mood(+keyword)，输出 `{"src": "<相对路径>", "caption": str}` 或 `{"kaomoji": "(¬_¬)"}`；`_index.json` 结构 `[{"file","mood","caption","tags"}]`。

- [x] **Step 1: 生成占位图脚本**

```python
# scripts/gen_placeholder_memes.py —— 纯文本 SVG，零依赖，可重跑
import os
MOODS = {"urge": ("催促", "还不打卡？(・`ω´・)"),
         "praise": ("夸奖", "干得漂亮！(๑•̀ㅂ•́)و✧"),
         "disappointed": ("失望", "唉…(´-ι＿-｀)"),
         "angry": ("暴怒", "说好的打卡呢！(╬￣皿￣)=○"),
         "cute": ("卖萌", "求求啦～(｡•ᴗ•｡)♡"),
         "celebrate": ("庆祝", "太强了！(ﾉ≧∀≦)ﾉ")}
BASE = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
                    "skills", "meme-buddy", "assets", "memes")
def main():
    for mood, (zh, kao) in MOODS.items():
        d = os.path.join(BASE, mood); os.makedirs(d, exist_ok=True)
        svg = (f'<svg xmlns="http://www.w3.org/2000/svg" width="480" height="480">'
               f'<rect width="100%" height="100%" fill="#1e1f24"/>'
               f'<text x="240" y="190" font-size="72" fill="#ffd166" text-anchor="middle" '
               f'font-family="sans-serif">{zh}</text>'
               f'<text x="240" y="300" font-size="40" fill="#fff" text-anchor="middle" '
               f'font-family="sans-serif">{kao}</text></svg>')
        open(os.path.join(d, "01.svg"), "w", encoding="utf-8").write(svg)
    os.makedirs(os.path.join(BASE, "user"), exist_ok=True)
    print("generated:", BASE)
if __name__ == "__main__":
    main()
```

Run: `python3 scripts/gen_placeholder_memes.py` → 六目录各出现 `01.svg` + `user/` 空目录 + `_index.json`（内容为六条记录：`{"file":"urge/01.svg","mood":"urge","caption":"还不打卡？(・`ω´・)","tags":["催促"]}`…celebrate 各一条）。

- [x] **Step 2: 写 SKILL.md（全文）**

```markdown
---
name: meme-buddy
description: 当需要挑一张符合当下情绪的表情包发给别人时使用（催促/夸奖/庆祝/失望/卖萌/暴怒六类情绪）。被习惯监督消息流程调用，也可独立用于日常斗图。
---

# 表情包引擎

按情绪选一张表情包。选图顺序（PRD §4.5）：

1. 读本 skill 的 `assets/memes/_index.json`。
2. mood 必须是固定枚举：urge / praise / disappointed / angry / cute / celebrate。
3. 本地匹配：keyword 命中 tags 或 caption 优先；否则取该 mood 目录随机一张；
   避开 `persona_state.recent_memes` 里最近 3 张（在 state/habits.json）。
4. 用户自备：`assets/memes/user/` 里的图未进索引时按 cute 兜底，首次使用时
   询问用户一句"这张配什么情绪？"并把答案写回 _index.json。
5. 降级链：本地包不可用（文件缺失/为空）时，直接输出 kaomoji 映射：
   urge→(・`ω´･)  praise→(๑•̀ㅂ•́)و✧  disappointed→(´-ι＿-｀)
   angry→(╬￣皿￣)=○  cute→(｡•ᴗ•｡)♡  celebrate→(ﾉ≧∀≦)ﾉ
   并照常发出文字消息——图是增强，不是依赖。
6. 输出约定：`{"src": "assets/memes/<mood>/xx.svg", "caption": "..."}` 或
   `{"kaomoji": "(…)"}`；调用方把它与消息正文一起交给宿主投递。
7. 发送成功后把所选文件名追加进 `persona_state.recent_memes`（保最长 3 条）。

在线 provider（Giphy/ALAPI）为 M2 能力，本版本不存在，不要尝试联网搜图。
```

- [x] **Step 3: 验证**

Run: `python3 scripts/gen_placeholder_memes.py && ls skills/meme-buddy/assets/memes/*/ | head` + 人工核对 SKILL.md frontmatter 仅 name/description 两键。
Expected: 六个 mood 目录各含 01.svg；frontmatter 合规。

- [x] **Step 4: Commit**

```bash
git add skills/meme-buddy scripts/gen_placeholder_memes.py
git commit -m "feat: meme-buddy——本地表情包底座/kaomoji 降级/user 目录机制"
```

---

### Task 5: habit-checkin skill（意图协议 + schema 文档）

**Files:**
- Create: `skills/habit-checkin/SKILL.md`
- Create: `skills/habit-checkin/references/state-schema.md`

**Interfaces:**
- Consumes: `scripts/state.py` 的 `apply_checkin/find_habit/validate/save`（SKILL.md 指示 agent 通过 shell 调用：`python3 -c "..."` 或写小脚本调用；宿主均有 shell 工具）。
- Produces: 打卡/查询/请假/补卡/配置五类意图的处置协议（供 enforcer 与宿主触发路由）。

- [x] **Step 1: 写 SKILL.md（全文核心，意图→动作映射表）**

```markdown
---
name: habit-checkin
description: 当用户消息涉及习惯打卡、查询打卡进度、请假、补卡、新建或修改习惯配置、切换监督档位（严厉/宽松/自由）时使用。用户说"打卡/今天跑了/睡了✅/我这周怎么样/补昨天的/监督我早睡/改成严厉"等都必须触发本 skill。
---

# 习惯打卡入口

状态文件：仓库根的 `state/habits.json`（schema 见 references/state-schema.md）。
所有记账必须先改内存 dict、再 `save()` 原子写回，改完跑 `validate` 自检。

## 意图 → 动作

| 意图 | 判定线索 | 动作 |
|---|---|---|
| 打卡 | "打卡/✅/完成了/达标"+习惯名 | `apply_checkin(state, h_id, today, note, proof, now)`；回复风格按 effective_level |
| 带凭证 | 消息含图片 | 同上，proof=True（图留在 IM，状态只记布尔） |
| 缺凭证 | require_proof=True 且纯文字 | 温和索要一次（"图呢？"）；用户坚持则 proof=False 照常入账 |
| 查询 | "这周/连续几天/怎么样" | 读 stats/records，按档位人设语气汇报 |
| 请假 | "请假/出差/暂停"+日期范围 | 写 pauses；确认区间内 due 日免罚 |
| 补卡 | "补昨天" | apply_checkin 会校验 chill 档+freeze_left；拒绝时解释规则 |
| 配置 | "监督我XX/建个打卡/改成严厉/窗口改到X" | 按 schema 写 habits 条目；缺省值：每天、07:00–22:00、继承全局档、无凭证、周目标5；**必须先复述确认摘要再落库** |
| 无关 | 与习惯无关 | 不抢话，交回宿主 |

## 回复风格（PRD §4.2/§4.3）
- strict（教导主任）：毒舌简短；打卡成功 ≤2 句 + celebrate 图。
- chill（损友）：热烈吹捧 ≤2 句 + praise/cute 图。
- 打卡成功后调用 meme-buddy 配图；自由反馈不超 2 句。
- 删除习惯需二次确认；一切配置变更回显摘要。
```

- [x] **Step 2: 写 references/state-schema.md**

内容 = PRD §3.3 的 schema 全文拷贝 + 三条操作约定（原子写、改后 validate、records 只留 90 天由 tick 清理）+ `apply_checkin/find_habit` 的函数签名与返回值说明（从 Task 2 Interfaces 拷贝）。

- [x] **Step 3: 验证**

Run: `python3 scripts/validate_state.py`（确认没动坏状态）+ 人工核对 description 触发词覆盖：打卡/查询/请假/补卡/配置五类。
Expected: OK；触发词齐全。

- [x] **Step 4: Commit**

```bash
git add skills/habit-checkin
git commit -m "feat: habit-checkin——五类意图协议与 schema 参考文档"
```

---

### Task 6: habit-enforcer skill（tick 编排 + 两档人设模板池 + 升级规则）

**Files:**
- Create: `skills/habit-enforcer/SKILL.md`
- Create: `skills/habit-enforcer/references/personas.md`
- Create: `skills/habit-enforcer/references/escalation.md`

**Interfaces:**
- Consumes: `scripts/tick_check.py` 输出契约（Task 3）、meme-buddy 选图协议（Task 4）、personas 模板变量。
- Produces: 标准 tick 执行协议（宿主 cron prompt 引用它；模板槽位 `{{name}}/{{habit}}/{{streak}}/{{deadline}}/{{week_rate}}`）。

- [x] **Step 1: 写 SKILL.md（全文）**

```markdown
---
name: habit-enforcer
description: 仅在定时/cron tick（监督 tick）或生成周报/断链日结时使用；处理催促升级、断链审判、档位降级通知。用户即时消息不要用本 skill（走 habit-checkin）。
---

# 习惯监督引擎（tick 流程）

每次 tick 严格按以下顺序：

1. 在仓库根执行 `python3 scripts/tick_check.py --commit --now <当前ISO时间>`，
   得到 `{"now":..., "actions":[...]}`。**不要自行心算时间或轮次——脚本是唯一事实源。**
2. actions 为空 → 本轮结束，不输出任何消息。
3. 对每个 action：
   a. 按 references/personas.md 选当前档位（effective_level）对应 stage 的模板
      （避开 persona_state.recent_templates 最近 5 条；选完追加并裁剪到 5）。
   b. 用 action.slots 填槽（{{name}}/{{habit}}/{{streak}}/{{deadline}}/{{week_rate}}）。
   c. 调 meme-buddy：nudge 用 action.mood；weekly_report 用 celebrate（chill）/angry（strict 公审）；
      level_down 用 cute。
   d. 输出最终消息（文字 + 图），交宿主投递。多 action 时合并为最多 2 条消息，
      按 type 排序：level_down → nudge → weekly_report。
4. `daily_close`：**不发消息、不配图**——断链已由脚本记录归档（commit 时已把 streak 归零），
   处刑在周日公审周报（M2 起为次日日报）。它没有 `mood` 字段，你不得为它渲染任何消息。
5. level_down 动作：脚本已在 commit 时把该习惯降为 chill 并写明理由，你只需
   以当前人设告知用户（例："连续 3 天没理我，先撤为敬，改宽松档了"）。
6. 免打扰与 ≤4 条上限由脚本保证，你不做额外判断，也不要在免打扰时段补发。
```

- [x] **Step 2: 写 references/personas.md（两档完整模板池，strict 10 条 / chill 10 条）**

```markdown
# 人设与模板池

变量：{{name}} {{habit}} {{streak}} {{deadline}} {{week_rate}}
规则：模板是骨架，可在语气细节上自由发挥，但不得偏离人设；同 stage 内轮换使用。
`daily_close` 是静默记账（无 mood、不发消息），**没有模板**；strict 的断链处刑落在 `weekly_report` 公审模板里。

## 🔴 strict —— 教导主任
- remind（urge）：{{name}}，{{habit}}的窗口开了，{{deadline}}关。我知道你看见了。
- remind（urge）：今日份{{habit}}已上线。你的连续 {{streak}} 天还热乎着，别让它凉了。
- first（urge）：一小时了。{{name}}，{{habit}}呢？我小本本已经翻开第 {{streak}} 页了。
- first（urge）：别人都在坚持，你在干嘛？{{habit}}，现在就是最好的时间。
- warn（disappointed）：三小时了。我再提醒一次：{{habit}}，窗口 {{deadline}} 关。
- warn（disappointed）：我已经在写今天的评语了，希望不用写"{{name}}，{{habit}}，缺席"。
- final（angry）：最后通牒。{{deadline}}后{{habit}}断链，明天日报见，{{name}}。
- final（angry）：我数到三。三。{{habit}}！打卡！现在！
- weekly_report（angry）：本周公审：{{week_rate}}。哪些天在摸鱼我都有记录，下周别让我请家长。
- weekly_report（angry）：记录在案：本周 {{week_rate}}。缺席的那几天我都记着，下周再这样我就请家长了。

## 🟡 chill —— 损友
- remind（cute）：{{name}}～{{habit}}搞起？就差你这一下了(｡•ᴗ•｡)
- remind（cute）：友情提醒，{{habit}}窗口 {{deadline}} 关。搞不搞随意，我就问问～
- remind（cute）：今天{{habit}}了吗？没有的话……也不是不行，但我会在周报里嘀咕的哦。
- lastcall（cute）：最后问一次～{{habit}}窗口 {{deadline}} 就关了，现在搞定还来得及。
- lastcall（cute）：窗口快关啦，{{name}}。{{habit}}，一分钟的事儿，冲一个？
- 打卡成功（praise）：可以啊{{name}}！{{habit}}连续 {{streak}} 天，卷王认证！
- 打卡成功（praise）：{{streak}} 天了！这波必须吹，周末奖励自己一顿好的。
- 打卡成功（cute）：记上了记上了～{{habit}} +1，距离周目标又近一步。
- weekly_report（celebrate）：本周{{week_rate}}，稳中向好！下周继续保持，别骄傲啊喂。
- weekly_report（celebrate）：周报出炉：{{week_rate}}。虽然你嘴上嫌我烦，身体倒是挺诚实嘛。
```

- [x] **Step 3: 写 references/escalation.md**

内容（PRD §4.2 硬约束的操作员视角重述）：stage 机（strict：remind/first/warn/final 四轮；chill：remind/lastcall 两轮；含时刻表与免打扰顺延/提前规则）、每日 ≤4 条由脚本 nudge_state 保证、免打扰 22:30–24:00 与 00:00–07:00 两段绝对优先（`daily_close` 豁免但静默）、幂等规则、熔断（no_response_days≥3 → level_down，M1 即生效）、chill 周目标制判定说明（week_count ≥ weekly_goal 即达标，周报呈现）。

- [x] **Step 4: 验证**

Run: `python3 -m unittest discover tests -v`
Expected: 全 PASS（本任务纯文档，跑测试确认无回归）+ 人工核对模板变量与 tick slots 一致。

- [x] **Step 5: Commit**

```bash
git add skills/habit-enforcer
git commit -m "feat: habit-enforcer——tick 编排协议与两档人设模板池"
```

---

### Task 7: 宿主接线文档 + README + 场景 1–5 脚本级验收

**Files:**
- Create: `docs/host-hermes.md`、`docs/state-schema.md`（从 Task 5 的 references 拷贝并加"人工检查清单"小节）、`README.md`

**Interfaces:**
- Consumes: 全部前序产物。
- Produces: 10 分钟 Hermes 接线验收清单（PRD §5.3）。

- [x] **Step 1: 写 docs/host-hermes.md（含可复制的命令与 tick prompt）**

核心内容：安装（拷贝 skills/ 三目录到 Hermes skills 目录；仓库放服务器任意路径）；建 cron：

```bash
/cron add "every 30m" "执行习惯监督 tick：cd 到 peck-food 仓库根，运行 python3 scripts/tick_check.py --commit，然后按 skills/habit-enforcer 的协议处理输出；actions 为空则保持沉默。" --skill habit-enforcer --skill meme-buddy --workdir /srv/peck-food
```

渠道绑定说明（结果投递回来源会话）；无 agent 模式降级（`hermes cron` 的 script payload 直跑 `tick_check.py`，输出 JSON 原样投递，适合省钱挂机）；10 分钟验收清单 = PRD §8 场景 1–5 的手工版（含"如何把 habits.json 里窗口临时改成 2 分钟后以快速观察催促"的调试技巧）。

- [x] **Step 2: 写 README.md**

内容：一句话介绍、能力假设 H1–H4（PRD §2.2 拷贝）、快速开始（3 步：放仓库→装 skills→建 cron，指向 docs/host-hermes.md）、`scripts/` 三个命令的一句话说明、表情包版权声明（内置仅自绘/CC0 占位，鼓励往 assets/memes/user/ 丢自己的图）、M1 范围与 M2 预告一行。

- [x] **Step 3: 场景 1–5 脚本级自动化验收（写成可重复的冒烟脚本）**

Run（逐条执行并核对输出）：

```bash
# 场景1 建习惯（配置走 checkin 的 LLM 协议，脚本级验证 schema 合法）
python3 scripts/validate_state.py                     # → OK

# 场景2 催促链+幂等（用测试夹具状态驱动，直接跑单测即等价）
python3 -m unittest tests.test_tick -v                # → 含 warn/final/幂等/上限 全 PASS

# 场景3/4/5 打卡/streak/断链/补卡券
python3 -m unittest tests.test_checkin -v             # → 全 PASS

# 全量回归
python3 -m unittest discover tests -v
```

Expected: 三组全 PASS；`git status` 干净。

- [x] **Step 4: Commit**

```bash
git add docs README.md
git commit -m "docs: Hermes 接线指南/state-schema/README 与 M1 验收清单"
```

---

## Self-Review 结果

1. **Spec 覆盖**：PRD §8 场景 1（Task 5 配置协议+Task 1 schema）、2（Task 3）、3（Task 2+5）、4（Task 3 daily_close+level_down）、5（Task 2 补卡券）；§4.2 两档行为（Task 3 判定+Task 6 模板）；§4.5（Task 4）；§5.3 Hermes（Task 7）。M1 不含：free 档/日报/成就/在线 provider（按 PRD §7 归 M2）——已确认无缺漏。
2. **占位符扫描**：模板池 20 条（strict 10 / chill 10，daily_close 无模板）全部成文；tick prompt、SKILL.md 全文给出；无 TBD/TODO。
3. **类型一致性**：`per_habit_day` 键格式统一为 `<habit_id>@<YYYY-MM-DD>`；action 的 stage 枚举（remind/first/warn/final/lastcall）在 Task 3/6 一致；mood 枚举六值在 Task 3/4/6 一致（`daily_close` 无 mood）；模板变量 `{{name}}/{{habit}}/{{streak}}/{{deadline}}/{{week_rate}}` 与 slots 字段名对应。
